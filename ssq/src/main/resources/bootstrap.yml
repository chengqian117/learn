server:
  port: 8001
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka,http://localhost:8762/eureka
spring:
  application:
    name: ssq
  profiles:
    active: test
  cloud:
    config:
      name: ssq
      profile: test
      label: master
      discovery:
        enabled: true
        service-id: config
